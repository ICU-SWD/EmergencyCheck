# คู่มือติดตั้งระบบเช็คอุปกรณ์/ยาฉุกเฉิน สำหรับหน่วยงานอื่น

ระบบมี 2 ส่วน:

| ส่วน | อยู่ที่ไหน | หน้าที่ |
|---|---|---|
| หลังบ้าน | Google Sheet + Apps Script ที่ผูกกับชีต | เก็บรายการ/รายชื่อ/ประวัติ, แจ้งเตือน LINE |
| หน้าเว็บ | รีโปนี้ (GitHub Pages) | ฟอร์มให้พยาบาลกรอก |

รายการที่ต้องเช็ค คัน/จุด และรายชื่อผู้บันทึก **อยู่ในชีตทั้งหมด ไม่ต้องแก้โค้ด**

---

## ขั้นที่ 1 — ทำสำเนาชีตต้นแบบ

1. เปิดชีตต้นแบบ แล้ว File → Make a copy (โค้ด Apps Script จะติดมาด้วย)
2. เปิด Extensions → Apps Script → ⚙️ Project Settings → **Script Properties**
   ถ้ามีค่าใดติดมา (token ของหน่วยงานต้นทาง) ให้ **ลบทิ้งทั้งหมด**
3. ถ้าในโปรเจกต์ Apps Script ยังมีไฟล์ `MasterData.gs` ให้ลบทิ้ง (ระบบไม่ได้ใช้ตอนทำงานจริง
   และถ้ากดรัน `seedMasterData()` จะเขียนทับชีต `Carts`, `Items`, `Staff` ด้วยข้อมูลของหน่วยงานต้นทาง)

## ขั้นที่ 2 — ใส่ข้อมูลของหน่วยงาน

### แท็บ `Carts` — คัน/จุดที่เช็ค

| CartID | CartName | Mode |
|---|---|---|
| `ER_CART1` | รถ Emergency ER | `checklist` |
| `ER_FRIDGE` | ตู้เย็นยา ER | `log` |

- **CartID**: ภาษาอังกฤษ/ตัวเลข/`_` เท่านั้น ไม่มีช่องว่าง
- **CartName**: ชื่อที่แสดงบนหน้าเว็บ และใช้เป็นชื่อแท็บสรุปของคันนั้น (ระบบสร้างแท็บให้อัตโนมัติตอนบันทึกครั้งแรก)
- **Mode**
  - `checklist` — เทียบกับจำนวนที่ต้องมี แล้วสรุป "ครบถ้วน / ไม่ครบ"
  - `log` — จดจำนวนอย่างเดียว ไม่ตัดสินครบ/ไม่ครบ (สถานะ "บันทึกแล้ว")

### แท็บ `Items` — รายการในแต่ละคัน

| CartID | ItemID | ItemName | RequiredQty | SortOrder |
|---|---|---|---|---|
| `ER_CART1` | `adrenaline` | Adrenaline 1mg/ml Inj. | 10 | 1 |

- **CartID** ต้องตรงกับแท็บ `Carts`
- **ItemID** ภาษาอังกฤษ ไม่ซ้ำกันภายในคันเดียวกัน
- **RequiredQty** จำนวนที่ต้องมี (คันแบบ `log` ใส่ 0)
- **SortOrder** ลำดับที่แสดงบนหน้าจอ

> ⚠️ เมื่อเริ่มใช้งานแล้ว **อย่าแก้ CartID / ItemID เดิม** (ประวัติจะไม่ต่อกัน) ถ้าจะเปลี่ยนชื่อ ให้แก้ที่ CartName / ItemName
> ถ้าแก้ CartName ระบบจะสร้างแท็บสรุปใหม่ตามชื่อใหม่

### แท็บ `Staff` — รายชื่อผู้บันทึก

คอลัมน์ `StaffName` ใส่ชื่อ 1 คนต่อ 1 แถว (ถ้าว่าง dropdown ผู้บันทึกจะไม่มีตัวเลือก และบันทึกไม่ได้)

### แท็บอื่น

- `Log` — ประวัติแบบละเอียด ระบบเขียนเอง (เก็บแถวหัวตารางไว้)
- แท็บชื่อตามคัน — ระบบสร้างเอง

## ขั้นที่ 3 — ตั้งค่าแจ้งเตือน LINE (ไม่บังคับ)

ใช้ LINE Official Account (Messaging API) ตั้งค่าใน Apps Script → Project Settings → Script Properties
**แยกตาม CartID** ของแต่ละคัน:

| Property | ค่า |
|---|---|
| `LINE_TOKEN_<CartID>` เช่น `LINE_TOKEN_ER_CART1` | Channel access token |
| `LINE_GROUP_<CartID>` เช่น `LINE_GROUP_ER_CART1` | Group ID ของกลุ่มที่จะส่งเข้า |

คันไหนไม่ได้ตั้งค่า ระบบจะข้ามการแจ้งเตือนของคันนั้น (บันทึกลงชีตได้ตามปกติ)

## ขั้นที่ 4 — บันทึกลง GitHub (ไม่บังคับ แนะนำให้ข้าม)

ถ้าไม่ตั้ง `GITHUB_TOKEN` / `GITHUB_OWNER` / `GITHUB_REPO` ระบบจะข้ามส่วนนี้ ข้อมูลยังอยู่ในชีตครบ
ถ้าจะใช้: ไฟล์ log มีชื่อผู้บันทึก ควรใช้ repo แบบ private แยกจากหน้าเว็บ (`GITHUB_BRANCH` ไม่ตั้ง = `main`)

## ขั้นที่ 5 — Deploy Apps Script เป็น Web app

1. Apps Script → **Deploy → New deployment** → ประเภท **Web app**
2. Execute as: **Me** / Who has access: **Anyone**
3. กด Deploy แล้วอนุญาตสิทธิ์ (Authorize) ตามที่ Google ถาม
4. คัดลอก **Web app URL** (ลงท้ายด้วย `/exec`)

> แก้โค้ดภายหลัง ต้อง Deploy → Manage deployments → ✏️ → Version: New version ไม่อย่างนั้น URL เดิมจะยังใช้โค้ดเก่า

## ขั้นที่ 6 — หน้าเว็บ

1. สำเนารีโปนี้ (Fork หรือ Use this template) แล้ว **ลบโฟลเดอร์ `logs/`** (เป็นข้อมูลของหน่วยงานต้นทาง)
2. แก้ `index.html`
   - `var API_BASE = "..."` → ใส่ Web app URL จากขั้นที่ 5
   - `<p class="org-name">...</p>` → ชื่อหน่วยงาน/โรงพยาบาล
   - ถ้าเวรไม่ใช่ เช้า/บ่าย/ดึก ให้แก้ตัวเลือกใน `<select id="shiftSelect">`
3. เปลี่ยน `logo.png` เป็นโลโก้ของหน่วยงาน (แนะนำขนาดไม่เกิน ~200 KB)
4. ลิงก์ลัดต่อคัน: โฟลเดอร์ `drug/`, `equip/`, `fridge/` พาไปที่ `?cart=<CartID>` ของต้นทาง
   ให้แก้ CartID ในไฟล์ `index.html` ของแต่ละโฟลเดอร์ หรือลบทิ้งแล้วใช้ลิงก์ `https://<เว็บ>/?cart=<CartID>` แทน
5. GitHub → Settings → Pages → Source: branch `main` / root

## ตรวจสอบหลังติดตั้ง

- เปิด Web app URL ตรงๆ ต้องเห็น `{"status":"ok", ...}`
- เปิดหน้าเว็บ ต้องเห็นคัน/รายการ/รายชื่อของหน่วยงาน
- ลองบันทึก 1 ครั้ง → ดูว่ามีแถวใหม่ในแท็บ `Log` และแท็บชื่อคัน และ LINE แจ้งเตือน (ถ้าตั้งค่าไว้)
- ถ้า LINE ไม่มา: Apps Script → Executions ดูข้อความ log (`ข้าม LINE notify` = ยังไม่ได้ตั้ง Script Properties ของคันนั้น)
